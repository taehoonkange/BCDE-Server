# 공통
spring:
  config:
    activate:
      on-profile: "common"
  servlet:
    multipart:
      max-file-size: 50MB     #업로드 파일 크기 제한


  mvc:
    pathmatch:
      matching-strategy : ant_path_matcher

logging:
  level:
    org:
      hiberante:
        SQL: debug

    com:
      bcdeproject : info


