spring:
  profiles:
    group:
      "local": "local, common"
      "development": "development, common"

#  datasource:
#    driver-class-name: org.h2.Driver
#    username: sa

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url : jdbc:mysql://bcde-rds-instance.c2xonyrh3m6y.ap-northeast-2.rds.amazonaws.com:3306/bcde_test?characterEncoding=UTF-8&serverTimezone=UTC&autoReconnect=true
    username : admin
    password : bcdetest

  data:
    web:
      pageable:
        default-page-size: 20  #페이징 할 때 기본값, 20개씩 조회

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  servlet:
    multipart:
      max-request-size: 5MB  #업로드 파일 크기 총량 제한
      max-file-size: 2MB     #업로드 파일 크기 제한



  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        user_sql_cooments: true


        default_batch_fetch_size: 100 #배치 사이즈 (Collection 조회 최적화)

    hibernate:
      ddl-auto: create


    open-in-view: false #OSIV 사용하지 않기


  h2:
    console:
      enabled: true





logging:
  level:
    p6spy: info
    org:
      apache:
        coyote:
          http11: OFF #debug

      hiberante:
        SQL: warn



image:
  dir: D:\bcdeImg\

---
### JWT
jwt:
  secret: 'Z87f78dygVAFGV87AY78YV87vuaaf12by98g4q93q0wva0djv05Y4Q98VRUVH38YcvhYF9843Y9hfojeovhq'

  access:
    expiration: 20000
    header: Authorization

  refresh:
    expiration: 30000
    header: Authorization-refresh

---
# 공통
spring:
  config:
    activate:
      on-profile: "common"
    servlet:
      multipart:
        max-request-size: 20MB  #업로드 파일 크기 총량 제한
        max-file-size: 2MB     #업로드 파일 크기 제한

---
# AWS S3에 필요한 정보
cloud:
  aws:
    region:
      static: ap-northeast-2
    s3:
      bucket: bcde-bucket
    stack:
      auto: false

logging:
  level:
    com:
      amazonaws:
        util:
          EC2MetadataUtils: error

---
# 로컬 환경
spring:
  environment: "local"
  config:
    activate:
      on-profile: "local"
  file-dir: /src/test/resources/static/files/

---
# 배포 환경
spring:
  environment: "development"
  config:
    activate:
      on-profile: "development"
  file-dir: /home/ec2-user/files/